# ⏱️ DANA Overlay Timer (v1.4.0)

Минималистичный таймер-оверлей для контроля циклов работы и отдыха под брендом **DANA SYSTEMS**.

---

## 🧠 Технологический стек

- **Python 3.x** — язык разработки
- **PyQt6** — GUI Framework
- **PyInstaller** — Binary Bundling

---

## ✨ Что нового в v1.4.0 (Final Polish)

- 🕵️ **Скрытые настройки**: Файл `settings.json` автоматически получает атрибут «Скрытый» в Windows (через `SetFileAttributesW` с флагом `0x02`). Файл создаётся рядом с исполняемым и скрывается при каждом сохранении настроек.
- 📍 **Память позиции**: Таймер запоминает, где вы его оставили на экране, и открывается там же.
- 💾 **Полная инициализация**: Программа сохраняет и восстанавливает время, раунды, масштаб и позицию.
- 🏷️ **Чистое имя**: Исполняемый файл — `Overlay_Timer_Pro.exe`.

---

## ✨ Основные фишки

- **Всегда сверху**: Окно не перекрывается другими приложениями.
- **Масштабирование**: Колесико мыши меняет размер оверлея (0.4x — 3.0x).
- **Динамическое форматирование**: Автоматическое переключение между `SS`, `MM:SS` и `HH:MM:SS`.
- **Цветовая индикация**:
  - 🟢 **WORK** — рабочий цикл.
  - 🟠 **PAUSE** — отдых.

---

## 🖱️ Управление

- **ЛКМ (зажать)**: Перемещение по экрану.
- **ЛКМ (двойной клик)**: Быстрый Старт / Стоп.
- **ПКМ (меню)**: Настройка времени (сек/мин/час), раундов и пункт **About** (v1.4.0 | DANA SYSTEMS).
- **Колесико**: Zoom (изменение размера).

---

## 🚀 Быстрый старт (для разработчиков)

1. Установка зависимостей:
   ```bash
   pip install PyQt6 PyInstaller
   ```

2. Сборка EXE с иконкой:
   ```bash
   pyinstaller --noconsole --onefile --icon="icon.ico" --name="Overlay_Timer_Pro" main.py
   ```

   > Убедитесь, что файл `icon.ico` находится в корне проекта.

---

## 📋 Project Meta

- **Версия**: 1.4.0
- **Бренд**: DANA SYSTEMS
- **Лицензия**: Проприетарная (см. [LICENSE](LICENSE))
